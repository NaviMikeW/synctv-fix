{{- $databasePassword := required "postgresql.password is required; provide a strong password through a protected values file or --set-string" .Values.postgresql.password -}}
apiVersion: v1
kind: Secret
metadata:
  name: synctv-secrets
type: Opaque
stringData:
  DATABASE_PASSWORD: {{ $databasePassword | quote }}
